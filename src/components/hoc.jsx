const SectionWraper = ({ children }) => {
  return (
    <div className="flex items-center justify-center xl:px-12 lg:px-6 md:px-3 px-2 w-full">
      {children}
    </div>
  );
};
export default SectionWraper;
